Tick 대신 Timer를 사용해서 MovingPlatform 클래스와 RotatingPlatform 클래스 생성
StageManager 클래스를 생성해서 배치해서 무작위 공간내에 무작위 발판 설정한 개수만큼 생성
UPROPERTY로 구현한 부분은 언리얼 에디터의 Details에서 변수 설정 가능.
플레이 도중 Details에서 직접 값을 수정하여 실시간으로 적용 가능.
